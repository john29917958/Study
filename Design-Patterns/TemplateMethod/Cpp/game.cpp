#include "game.h"

std::unique_ptr<character> game::ray_cast_character(vector origin, vector end)
{
	//TODO: Checks if any character located on the line from origin to end.
	return nullptr;
}

std::unique_ptr<character> game::ray_cast_character(vector origin, float radius)
{
	//TODO: Checks if any character located within the circle at origin with specific radius.
	return nullptr;
}
